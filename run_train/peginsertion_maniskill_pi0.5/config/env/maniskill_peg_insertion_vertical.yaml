env_type: maniskill

total_num_envs: null
wrap_obs_mode: simple
use_wrist_image: False
auto_reset: True
ignore_terminations: False
use_rel_reward: False
# Re-anchor the pd_ee_target_delta_pose controller's _target_pose to the actual EE pose
# once per action chunk during eval, preventing open-loop target drift into OOD.
sync_target_pose_per_chunk: True
reward_mode: raw
action_scale: 1.0
seed: 0
group_size: 1
use_fixed_reset_state_ids: False
max_steps_per_rollout_epoch: 600
max_episode_steps: 600
task_description: "insert the blue peg vertically into the orange hole"
reset_options:
  randomize_initial_poses: True

video_cfg:
  save_video: False
  info_on_video: True
  video_base_dir: ${runner.logger.log_path}/video/train

enable_offload: False

init_params:
  id: PegInsertionVertical-v1
  num_envs: null
  obs_mode: "rgb"
  robot_uids: panda_wristcam
  control_mode: null
  sim_backend: "gpu"
  reward_mode: normalized_dense
  max_episode_steps: ${..max_episode_steps}
  render_mode: all
  sensor_configs:
    shader_pack: "default"
    hand_camera:
      width: 224
      height: 224
